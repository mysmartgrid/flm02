diff --git a/hostsoftware/libhexabus/CMakeLists.txt b/hostsoftware/libhexabus/CMakeLists.txt
index 0644019..98100f7 100644
--- a/hostsoftware/libhexabus/CMakeLists.txt
+++ b/hostsoftware/libhexabus/CMakeLists.txt
@@ -81,10 +81,13 @@ include(CMakeBackwardCompatibilityCXX)
 # find libklio
 find_package(Libklio)
 if( NOT LIBKLIO_FOUND)
-  message(FATAL_ERROR "libklio is required.You may set LIBKLIO_HOME")
+  #message(FATAL_ERROR "libklio is required.You may set LIBKLIO_HOME")
+  message(WARNING "libklio not found. hexalog will be disabled")
 endif( NOT LIBKLIO_FOUND)
 
-FIND_PACKAGE(Sqlite)
+if( LIBKLIO_FOUND )
+  FIND_PACKAGE(Sqlite)
+endif( LIBKLIO_FOUND )
 # We depend on boost >= 1.46.1 because of the updated filesystem
 # library.
 SET(Boost_ADDITIONAL_VERSIONS "1.46.1" "1.47" "1.47.1" "1.48.0" "1.48"
diff --git a/hostsoftware/libhexabus/src/CMakeLists.txt b/hostsoftware/libhexabus/src/CMakeLists.txt
index 862b863..2beb917 100644
--- a/hostsoftware/libhexabus/src/CMakeLists.txt
+++ b/hostsoftware/libhexabus/src/CMakeLists.txt
@@ -13,11 +13,16 @@ include_directories(
   ${CMAKE_CURRENT_SOURCE_DIR} 
   ${CMAKE_BINARY_DIR}/libhexabus
   ${Boost_INCLUDE_DIRS}
+)
+if(LIBKLIO_FOUND)
+include_directories(
   ${LIBKLIO_INCLUDE_DIR}
 )
+
 link_directories(
   ${SQLITE3_STATIC_LIBRARY_DIRS}
   )
+endif(LIBKLIO_FOUND)
 
 add_executable(hexaswitch ${hexaswitch_src})
 # Link the executables
@@ -35,6 +40,7 @@ target_link_libraries(hexaupload
   ${Boost_LIBRARIES}
 )
 
+if(LIBKLIO_FOUND)
 add_executable(hexalog ${hexalog_src})
 target_link_libraries(hexalog
   hexabus
@@ -44,11 +50,16 @@ target_link_libraries(hexalog
   ${LIBKLIO_LIBRARY}
   ${SQLITE3_LIBRARIES}
 )
+endif(LIBKLIO_FOUND)
 
 # add programs to the install target 
 INSTALL(PROGRAMS 
   ${CMAKE_CURRENT_BINARY_DIR}/hexaswitch
-  ${CMAKE_CURRENT_BINARY_DIR}/hexalog
   ${CMAKE_CURRENT_BINARY_DIR}/hexaupload
   DESTINATION bin)
 
+IF(LIBKLIO_FOUND)
+INSTALL(PROGRAMS
+  ${CMAKE_CURRENT_BINARY_DIR}/hexalog
+  DESTINATION bin)
+ENDIF(LIBKLIO_FOUND)
